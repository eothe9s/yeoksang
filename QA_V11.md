# 曆象 v11 QA

검증일: 2026-09-07

## 1. 자동 테스트

- 총 PASS: 73
- 기존 실행/시간표/백업/시험 회귀 테스트 통과
- 수면 경계, 과정별 마감, 실제 시간표 가용시간, 고정 일정, 자동배치, 주간 보정, 시험 시간붕괴, 오류 가중치, 종료 판정 테스트 통과
- v11 수용 테스트 통과
- v11 폐쇄루프 E2E 테스트 통과
- 동일 공개 함수명 재정의 체인 0개 검증 통과

실행 명령:

`node tests-v110.js`

## 2. 실제 2026-09-04 백업 복구 검증

실제 백업 `PROJECT_11122_backup_2026-09-04.json`을 v11 병합 함수에 투입해 확인했습니다.

병합 전 백업의 주요 데이터:

- 할 일: 32일 / 97개
- 날짜별 시간표: 92일
- 시험: 17개
- 대기함: 17개
- 컨디션: 7일
- 강의 상태: 132개
- 문제집 상태: 58개
- plannerMeta: 15일
- 마감 기록: 2개
- 자동화: 2개
- 최근 학습: 8개

v11 병합 후에도 위 개수가 감소하지 않았고 schema 14로 정상 이관됐습니다.

## 3. 계산 검증 (2026-09-07 기준)

근거가 있는 현재 과정만 계산했을 때:

- 남은 확정 학습량: 약 303.25–416.75시간
- 오늘 실제 시간표 최대 가용: 5.0시간
- 오늘 마감 필요량: 4.69–6.50시간 + 시간 미정 과정 별도
- 10/16까지 기간 평균 필요량: 7.58–10.42시간/일 + 시간 미정 과정 별도
- 상한 기준 오늘 계획 부족: 약 1.50시간

따라서 과거처럼 `오늘 필수 0.4–0.7h`로 과소표시하지 않습니다.

## 4. 자동배치 검증

- 90분 유연 작업 + 50분 블록 2개 → 50분 + 40분
- 두 번째 블록 잔여 10분 재사용 가능
- 연속 필요 작업은 사이에 공백이 있는 블록으로 강제 분할하지 않음
- 수면 경계 및 고정 일정과 충돌하는 자습 블록은 배치 대상으로 쓰지 않음

## 5. 브라우저 DOM/레이아웃 QA

Chromium 실제 DOM 실행으로 확인했습니다.

2048×1536:
- body scroll width = viewport width
- 메인 상단과 오늘 페이지 콘텐츠 rail 일치
- 기본 노출 모달 0개
- 할 일 모달 기본 display none
- 버튼 클릭 후 fixed overlay로 표시
- page error 0 / console error 0

1024×768 (iPad 가로 CSS viewport 모사):
- body scroll width 1024 = viewport 1024
- 상단 rail 783px = 오늘 페이지 rail 783px
- page error 0 / console error 0

768×1024 (세로 모사):
- body scroll width 768 = viewport 768
- 상단 rail 742px = 오늘 페이지 rail 742px
- page error 0 / console error 0

사용자가 제공한 실패 스크린샷의 핵심 증상인 `본문 아래에 편집 폼이 길게 노출됨`, `상단 앱과 하단 폼 가로폭 불일치`, `페이지 전체 수평 폭 붕괴`는 재현되지 않았습니다.

## 6. 정적 구조 감사

- HTML id: 353개 / 중복 0
- JS 직접 id 참조: 실 id 누락 0 (복합 CSS selector 제외)
- CSS `{}` 균형 정상
- index의 로컬 파일 참조 누락 0
- index/manifest/service worker에 v100 런타임 참조 0
- top-level 동일 함수명 중복 0
- `name = function(...)` 방식 공개 함수 덧씌우기 0
- PWA 이름/타이틀: 曆象 · 수능 만점 운영체제
- 구 project-11122 캐시는 service worker 활성화 시 제거 대상으로만 유지

## 7. 실제 iPad Safari에서 마지막으로 확인할 것

이 환경에서는 실제 사용자의 iPad Safari에 직접 손가락 입력을 할 수 없습니다. 따라서 배포 후 아래 4가지는 실제 기기에서 확인해야 합니다.

- 홈 화면 PWA 아이콘 갱신
- 가로/세로 회전 후 터치 영역 체감
- 기존 Safari localStorage 복구 후보 표시
- 며칠간 실제 입력했을 때 2–3분 내 일상 기록이 가능한지

이 네 항목에서 문제가 발견되면 기능 추가가 아니라 QA 결함으로 수정하면 됩니다.
