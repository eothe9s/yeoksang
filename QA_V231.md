# 曆象 v2.3.1 검증 — 2026-09-16

## 자동 검증

Node VM에서 애플리케이션 스크립트 전체를 순서대로 불러오고 모의 DOM·저장소로 36개 시나리오를 검증했습니다. 실제 브라우저 테스트와는 구분합니다.

- 지연 잔량 분리, 사용자의 재마감 적용, 오늘·기간 평균·주간 계산 일관성
- 공동 마감 시간 초과, 먼저 도래하는 마감, 강좌 공개 시작일, 소요시간 상한 위험, 미정 분량, 가용시간 0
- 명시된 재발 및 확인 통과 후 재발, 정상 확인 단계와의 구분, 해결 기록 제외, 과거 아카이브 제외
- 재가동 시 기존 핵심 과제/완료 인식, 사용자 우선순위 보존, 빈 계획 표시
- 90분 작업을 50+40분으로 배치하고 10분 재사용, 중복 배정 방지, 연속 작업의 틈새 분할 방지
- 계획 시간 중복 합산 방지, 겹치는 시간표의 가용시간 중복 합산 방지, 수면 제한, 면접일 0시간
- 마지막 10일 새 진도 자동배치 제한 및 복습 허용
- 시작 중 저장 쓰기 금지, 저장 실패 시 기존 원문 보존, 손상 JSON 덮어쓰기 방지
- 최종 검토가 끝나지 않은 분석 패키지 거부, 오래된 휴지통 보존, 고정 블록 기록 보존

## 제공된 백업 검증

YEOKSANG_backup_2026-09-10.json을 사용했습니다. 저장 공간이 없다고 가정한 최초 로딩에서도 저장 쓰기 0회이며 저장된 원문은 변하지 않았습니다.

- 날짜별 기존 할 일 내용 전체 비교: 108개, 완료 77개
- 시간표 전체 내용 비교: 92일, 1,061블록
- 강의 상태 154개, 문제집 상태 66개, 대기함·휴지통·생활 기록·사용자 교재·강의 보존
- 시험 18개의 식별자와 순서 유지

앱이 화면 표시용 빈 날짜를 추가하거나 기존 스키마의 기본 필드를 보완하는 것은 실제 학습 기록 생성과 구분했습니다. 시험 자료는 기존 정규화 과정을 사용하므로 위 검증은 모든 필드의 원문 동일성을 뜻하지 않습니다. 사용자 iPad의 현재 DB나 이후에 생성된 기록을 직접 열어 확인한 결과는 아닙니다.

## 배포 검사

모든 JavaScript 구문 검사, HTML/서비스워커의 로컬 자산 참조 확인, 모달 숨김 스타일 유지, ZIP 무결성 및 개인 백업 제외를 확인했습니다. 폴더 없이 같은 위치에 업로드하도록 패키징했습니다.

## 남은 확인

이 실행 환경에 브라우저 엔진이 없어 실제 Safari/Chromium 구동은 하지 못했습니다. iPad의 화면 밀도·터치·모달과 서비스워커 업데이트를 실기기에서 확인해야 합니다. 이번 판정 엔진은 마감 구간의 시간 총량을 검사하며 모든 작업의 세부 배치 가능성을 증명하는 스케줄러는 아닙니다.
