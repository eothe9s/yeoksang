# 曆象 v2.2 · 오답 분석 패키지

v2.1.1의 저장·시작 안전성은 유지하면서, 오답 입력을 사람이 다시 타이핑하지 않도록 `오답 분석 패키지` 증분 병합을 추가한 버전입니다.

## 핵심 흐름

1. 오답 PDF·사진·채점 기록을 ChatGPT에 전달합니다.
2. 필요하면 曆象의 `분석용 컨텍스트` JSON도 함께 전달합니다.
3. ChatGPT가 `yeoksang-analysis-package` JSON을 만듭니다.
4. `시험 → 오답 분석 가져오기`에서 JSON을 선택합니다.
5. 가져오기 전 미리보기를 확인한 뒤 병합합니다.

## 과거 오답과 현재 오답을 분리합니다

`historical-archive` 패키지는 과거 증거로 저장합니다. 과거 오답 60개를 오늘 미해결 문항 60개처럼 취급하거나 오늘 우선순위를 자동으로 올리지 않습니다. 새 시험에서 같은 오류가 재발했거나 현재 점검 대상으로 승격한 경우에만 현재 판단에 연결하는 것이 원칙입니다.

## 데이터 보호

- 같은 `packageId`는 두 번 가져올 수 없습니다.
- 가져오기 전에 현재 DB JSON을 다운로드합니다.
- 저장공간에 여유가 있을 때만 브라우저 내부에도 가져오기 직전 안전본을 추가합니다.
- 저장 실패 시 메모리 변경을 되돌리고 기존 브라우저 DB를 유지합니다.
- 원본 PDF는 localStorage에 넣지 않습니다. 파일명·페이지·분석 출처만 저장합니다.
- 사용자 작성 원인과 AI가 분류한 유형/패턴은 provenance로 구분합니다.

## 분석용 컨텍스트

`분석용 컨텍스트`는 앞으로 새 오답 파일을 분석할 때 필요한 현재 시험 기록, 기존 오답, 과거 오답 아카이브, 반복 패턴, 시험 규칙, 단원 점검 상태 등을 내보냅니다. 전체 DB 백업이 아니라 분석용 부분집합입니다.

## 주의

- 문항번호가 원본에서 가려져 있으면 추정하지 않고 `번호 미확인` 관찰로 보존합니다.
- 원본에 동일 시험명이 중복되거나 상충하는 경우 자동으로 합치지 않고 경고를 표시합니다.
- 과거 자료의 모델 분류는 참고값입니다. 원본에 사용자가 적은 직접 원인이 우선입니다.
