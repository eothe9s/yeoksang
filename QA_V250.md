# 曆象 v2.5 검증 기록 · v2.5.1 디자인 복원

## v2.5.1 변경 범위

2026-09-20: v2.4와 v2.5의 CSS 차이를 대조하여 전역 글자·색·버튼·완료 표시 변경을 철회했다. 데이터 및 계산 로직은 v2.5와 비교하여 변경되지 않았음을 파일 대조로 확인했다. 버전 표기·자산 URL·캐시 이름을 2.5.1로 갱신하고 자바스크립트 문법·로컬 자산 연결·ZIP 무결성을 검사했다. 별도 신규 기능을 추가하지 않았다.

아래 88개 회귀 검사와 브라우저 결과는 v2.5에서 수행한 검사다. 이번 디자인 복원에서는 이를 새로 실행한 것으로 세지 않는다. 브라우저 실행기가 없는 상태라 v2.5.1 실제 렌더링과 iPad Safari 화면은 확인하지 못했다. 작은 글씨와 낮은 대비에 관한 기존 접근성 지적 일부는 사용자 요청에 따른 스타일 복원으로 다시 해당할 수 있다.

---

검사 기간: 2026-09-18~20. 기준: v2.4 배포본. 브라우저 저장 키 `p11122_v60_db`와 스키마 14 유지.

## 검사 방식과 결과

- 기존 계산·배치·수면·시험·보고서 검사 59개와 v2.5 회귀 검사 29개, 합계 **88개 통과**. 삭제의 완주율 처리 등 사용자가 바꾼 요구는 새 계약에 맞춰 검사했다.
- 서비스 워커 모의 검사 6개 통과: 필수 자산 실패 시 새 캐시 폐기/이전 캐시 유지, 자동 강제 활성화 금지, 실제 버전 응답, 현재 버전 오프라인 페이지, 복구 페이지 구분.
- 실제 Chromium 브라우저 동작 검사 11개 통과: 전체 저장소의 무변경 시작, 대기함 왕복, 다음 날 완료, 편집 취소 확인, 두 탭의 쓰기 제한, 실패 변경 파일 내려받기, 외부 백업 검증 후 선택 사본 삭제, 저장 재시도, 재실행 지속, 표시 규칙.
- 제공된 원본·미저장 JSON에서 기존 할 일·시간표·대기함·강의 상태·문제집 상태·휴지통을 시작 시 보존하고, 시작 자체가 원본 저장값을 다시 쓰지 않음을 검사했다.
- 정상 서비스 워커 설치 후 네트워크 차단 상태에서 앱 재실행 통과. 공개 GitHub 운영 사이트와 실제 Safari는 검사하지 않았다.
- 1366px 화면의 오늘·시간표·학습·진도·시험·분석·설정, 820/683/390px의 주요 5화면을 점검했다. 검사된 좁은 폭에서 문서 가로 넘침 없음.
- 모달 포커스 진입/탭 순환/배경 포커스 차단, 키보드 취소 확인, 복사 거부 시 직접 선택 가능한 보고서 제공을 확인했다.
- 악성 속성 표식이 든 백업 ID는 거부되었고 실행되지 않았다. 우선순위 속성·프로토타입 키·과도한 자료 구조와 문항 번호 입력 경계도 검사했다. 이는 전체 보안 인증이 아니다.

## 감사 지적의 처리 범위

| 영역 | 이번 처리 | 남은 범위 |
|---|---|---|
| 자동 사본과 용량 | 새 전체 사본 중지, 내보내기 검증 뒤 선택 정리 | 기존 사본은 사용자 확인 전 보존; 현재 DB 자체가 커지면 여전히 저장 한계 가능 |
| 저장 실패와 복구 | 실패 payload 보관, 실패 후 성공 표시 차단, 주요 복합 변경의 단일 저장 | 기존 저장소 구조를 IndexedDB로 전면 교체하지 않음 |
| 두 탭과 병합 | Web Locks 쓰기 탭 제한, 저장 원문 비교, ID·삭제 충돌 | Web Locks 미지원 환경의 비교만으로 완전 동시 쓰기 원자성을 보장하지 않음; 이전 앱 탭은 닫을 것 |
| 완주율 | 원래 날짜 유지·같은 날 중복 방지·재마감 동일 결과 | 사용자가 명시한 삭제는 분모에서도 제거; 삭제로 비율이 오르는 것은 이번 요구사항 |
| 기록 계산 | 겹친 시간 중복 방지, 직접 실적 우선, 0점 구별, 입력 검증 | 실제 공부시간·수량·원인은 사용자가 기록한 범위만 알 수 있음 |
| 이월과 시험 연결 | 실제 배치 가능성 확인, 가져온 시험/재활성화 문항 정리 | 수동 override 허용; 기록을 지웠다고 이미 한 공부까지 취소하지 않음 |
| 렌더 성능 | 같은 화면 안의 반복 계산 캐시, 긴 선택·설정 목록 접기 | 전체 DB 직렬화 및 일부 큰 목록의 DOM 생성은 남음 |
| UI·접근성 | 가로 넘침, 모달 포커스, 취소 확인, 대비·버튼·그래프 표식 | 실기기 확대·VoiceOver·터치 제스처 등은 추가 확인 필요 |
| 업데이트 | 불완전 설치 거부, 이전 캐시 유지, 사용자 적용, 실제 SW 버전 | 운영 중 Safari/홈 화면 앱의 갱신은 미검증 |
| 코드 구조 | 기록 보호·공통 화면 동작을 분리하고 회귀 검사 추가 | 누적된 기존 버전별 스크립트 연결은 전면 재설계하지 않음 |

## 성능 수치의 해석

9월 20일 재개 후 회귀 88개와 서비스 워커 모의 검사 6개를 재실행해 통과했다. 브라우저 실행 파일과 검사 폰트가 임시 환경에서 사라져 브라우저 재검사는 실행하지 못했다. 위 브라우저 결과는 앞선 실행의 저장된 결과다. 마지막으로 완료 시간표의 교시 글자 대비를 CSS로 보완했으나 이 변경의 화면 재검사는 남아 있다. 앞선 자동 접근성 검사에서는 오늘 화면에 대비 지적 6개가 있었고, 다른 6개 기본 화면에서는 해당 검사 위반이 보고되지 않았다. 이를 전체 접근성 통과로 해석하지 않는다.

같은 제공 백업과 로컬 Chromium 환경에서 오늘 화면 갱신이 기존 약 1.7~2.0초에서 0.05~0.10초 수준으로 줄었다. 최종 검사 수치는 실행마다 변할 수 있다. 전체 DB 한 번 저장에는 대략 0.08~0.11초가 걸렸고 다른 화면에는 더 느린 표본도 있었다. iPad Safari의 속도를 보장하는 수치가 아니다.

## 데이터와 배포

- 원본 v2.4 ZIP은 수정하지 않았다. 기준 SHA-256: `13b9e603206112a344507f2561733195dc0ee90149672557bfcd9c3d01b045fd`.
- 안전본 삭제·강제 용량 실패·동시 탭 검사는 격리된 브라우저 복제본에서 실행했다. 사용자의 Safari/GitHub 기록에는 쓰지 않았다.
- 배포 ZIP에는 실제 개인 백업, 검사 스크린샷, 검사에서 쓴 원본 데이터가 없다. 빈 baseline과 기존 저장 키를 유지한다.
- 브라우저의 정확한 용량 한도는 이번 검사에서 측정하지 않았다. 동일한 저장소 사본만으로 Chromium에서 자연 용량 오류가 나지는 않았으며, 실패 복구 검사는 예외를 주입했다.
- 수능 만점에 충분한 학습량이나 학습 효과를 검증한 검사는 아니다. 필요량은 입력된 분량·마감일·가용시간으로 계산한다.

## 사용 후 빠른 확인

1. `v2.5`와 기존 기록을 확인한 뒤 전체 백업을 보관한다.
2. 미완료 할 일을 대기함에 넣어도 원래 날짜에 남는지, 다시 눌러도 중복되지 않는지 확인한다.
3. 저장 공간 오류가 남으면 README의 안전본 내보내기·파일 확인·선택 삭제·재시도 순서로 진행한다.
4. 새로고침 후 기록이 유지되는지 확인한다. 오류가 계속되면 원본/미저장 파일을 내보내고 웹사이트 데이터는 지우지 않는다.
