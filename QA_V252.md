# 曆象 2.5.2 — 20260923-evidence-1

## 기준과 범위

사용자가 제공한 Widget LIVE 2.5.1에 이전 반복 오답 포함 수정을 적용한 코드를 기준으로 수정했다.
검사용 데이터는 `YEOKSANG_backup_2026-09-23_DATES_FIXED(1).json`이며, 개인 백업은 이 배포 ZIP에 포함하지 않는다.

## 통과한 검사

| 검사 묶음 | 통과 |
|---|---:|
| 기존 시간표·학습 엔진 | 36 |
| 기존 저장·대기함·완주율·안전 처리 | 29 |
| 2.5.2 집계·삭제/복원·필터·위젯 API | 22 |
| 업데이트 상태 전이 | 8 |
| 서비스 워커 설치 실패·캐시·오프라인 처리 | 6 |
| Chromium 실제 브라우저 | 22 |
| 합계 | 123 |

- 첨부 백업의 경제: 17회, 20번 10/17회 오답. 현재 경제 재풀이 대기 7문항 유지.
- 점수만 입력한 시험은 그래프에는 남지만 반복 오답의 분모에는 들어가지 않는다.
- 명시적 오답 0개와 정규화 때문에 생긴 기본값 0을 구분한다.
- 최근 5회는 실제 응시일이 있는 최근 5회에서 선택하고, 날짜 미상·미래 기록은 섞지 않는다. 그중 번호가 없는 시험은 제외 수로 표시한다.
- 한두 회 기록도 숨기지 않는다. 3회/17회와 10회/17회의 색이 다르다.
- 같은 시험의 실제 응시 기록과 원본 분석을 함께 삭제/복원하고, 별도의 재응시는 유지한다.
- 삭제 후 옛 백업을 병합해도 삭제한 시험이 자동 복귀하지 않는다. 휴지통 복원으로 되돌릴 수 있다.
- 저장 용량 오류 때 삭제 전체가 되돌려지고, 미저장 변경은 내보내기용으로 유지된다.
- 기존 브라우저 DB와 파일을 초기화하지 않는다. 읽기/집계/렌더링 과정의 DB 쓰기 없음.
- 위젯 스냅샷 생성 코드·설정 키·전송 로직은 그대로 두고 서비스 워커 중복 등록만 제거했다.
- 실제 입력창, 분석 상세 열기, 전체/최근 전환, 삭제/복원 버튼을 브라우저에서 실행했다.
- 반복 오답 패널은 1366 / 820 / 390px에서 가로 넘침이 없었다.
- 기존 2.5.1에서 새 서비스 워커의 설치가 완료된 뒤 업데이트 버튼을 눌러 2.5.2 적용을 확인했다. 적용 후와 다음 새로고침 후 모두 배너가 숨겨졌고 기존 저장값은 바이트 단위로 같았다.
- 구버전은 같은 버전의 중복 업데이트 알림을 이미 표시할 수 있다. 새 설치가 끝나기 전에 그 알림을 누르면 구버전이 한 차례 더 열릴 수 있으므로 배포 완료 뒤 설정에서 수정본 식별값을 확인한다.
- JavaScript 전체 구문 검사와 HTML/서비스 워커 자산 연결 검사 통과.

## 검증 한계

실제 iPad Safari 기기와 사용자의 외부 위젯 서버는 직접 접속하지 않았다. 브라우저 검사는 격리된 로컬 Chromium에서 실행했다.
모든 상황에서 무결함이라는 보장은 아니다. 업데이트 전 최신 백업 보관을 권장하며, Safari 웹사이트 데이터를 지우지 않는다.
